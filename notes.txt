constructor 


its a special method/function which has same name 
as a class name and which must not have any 
return type.  
it will call automatically when we create instance/object. 

constructor is used to initialize instance variable. 


we have three types of constructor 
1) no argument - default 
2) parameterized constructor 
3) copy constructor 

Bowler(){

}
---------------------------------------------------------------

type variable ? 
local - method - block 
global - outside class 
instance - inside class - but not in method 
static - inside class or inside function - single copy 
int MIN_BAL = 5000; //global 
class Bank{
    private:
        int K;
    public:
       int bal; //instance variable
       static int BRANCH_CODE;//static - single copy  
       void wid(){
            int amt;//local 

       }
       dep();

};
class NBFC{

};
int main(){
    Bank ds;//bal
    Bank rs; //bal 

}


